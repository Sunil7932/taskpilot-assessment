# Frontend (server-side only — never exposed to the browser).
# Base URL of the TaskPilot API as seen from the Next.js server.
TASKPILOT_API_URL=http://localhost:8000
# Must match the backend's API_KEY. Injected server-side by the proxy routes.
TASKPILOT_API_KEY=change-me-in-production
